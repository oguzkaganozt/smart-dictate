#!/usr/bin/env python3
import os
import re
import subprocess
import sys
import time
import urllib.error
from pathlib import Path

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
import _voxtype_groq as groq  # noqa: E402

LOG = Path("/tmp") / "voxtype-summarize.log"
MAX_INPUT = 24000


def log(msg: str) -> None:
    try:
        with open(LOG, "a") as f:
            f.write(f"{time.strftime('%H:%M:%S')} {msg}\n")
    except Exception:
        pass


_cfg = groq.load_config("groq", "summarize")

MODEL = groq.resolve_model(_cfg["groq"], _cfg["summarize"], "SUMMARIZE")
ENDPOINT = groq.resolve_endpoint(_cfg["groq"], _cfg["summarize"], "SUMMARIZE")

DEFAULT_SYSTEM_PROMPT = (
    "You are a ruthless summarizer. Condense the text to its absolute "
    "essence — only key facts, numbers, and conclusions. "
    "Eliminate all examples, explanations, context, and repetition. "
    "Use a short descriptive title, max 6 words. "
    "Then output 3-6 bullets. Each bullet must be one short phrase or "
    "sentence, max 18 words. When a bullet has a clear topic, start with "
    "a 1-4 word bold label followed by a colon, like '- **Risk:** ...'. "
    "PRESERVE THE ORIGINAL LANGUAGE: if the input is in Turkish, output "
    "in Turkish; if English, output in English. "
    "Start the title on its own line prefixed with '## '. "
    "Output the title and bullet points only — "
    "no introductions, no commentary, no closing."
)
SYSTEM_PROMPT = (
    os.environ.get("SUMMARIZE_STYLE")
    or _cfg["summarize"].get("system_prompt", DEFAULT_SYSTEM_PROMPT)
)


def get_selected_text() -> str:
    for sel in ["primary", "clipboard"]:
        result = subprocess.run(
            ["xclip", "-o", "-selection", sel],
            capture_output=True, text=True, timeout=2.0,
        )
        if result.returncode == 0 and result.stdout.strip():
            log(f"read from {sel}: {len(result.stdout.strip())} chars")
            return result.stdout.strip()
    log("no text found in PRIMARY or CLIPBOARD")
    return ""


def summarize(text: str) -> str:
    api_key = groq.get_api_key(_cfg["groq"])
    if not api_key:
        raise RuntimeError("GROQ_API_KEY is not configured")
    payload = groq.build_payload(
        MODEL, SYSTEM_PROMPT, text,
        temperature=0.15,
        max_completion_tokens=groq.token_budget(text, SYSTEM_PROMPT),
    )
    output = groq.call_groq(
        ENDPOINT, api_key, payload, timeout=10.0, user_agent="voxtype-summarize/1.0",
    )
    log(f"summarize output: {len(output)} chars")
    return output


def get_mouse_position() -> tuple[int, int]:
    result = subprocess.run(
        ["xdotool", "getmouselocation"],
        capture_output=True, text=True, timeout=2.0,
    )
    x, y = 0, 0
    for part in result.stdout.strip().split():
        if part.startswith("x:"):
            x = int(part[2:])
        elif part.startswith("y:"):
            y = int(part[2:])
    return x, y


MD_RE = re.compile(r'\*\*(.+?)\*\*|`([^`]+)`|\*(.+?)\*')


def _insert_md(
    buffer: "GtkTextBuffer",
    it: "GtkTextIter",
    text: str,
    base_tag: "GtkTextTag",
    bold_tag: "GtkTextTag",
    code_tag: "GtkTextTag",
    italic_tag: "GtkTextTag",
) -> None:
    """Insert text with inline markdown support: **bold**, *italic*, `code`."""
    pos = 0
    for m in MD_RE.finditer(text):
        if m.start() > pos:
            buffer.insert_with_tags(it, text[pos : m.start()], base_tag)
        if m.group(1) is not None:
            buffer.insert_with_tags(it, m.group(1), base_tag, bold_tag)
        elif m.group(2) is not None:
            buffer.insert_with_tags(it, m.group(2), base_tag, code_tag)
        elif m.group(3) is not None:
            buffer.insert_with_tags(it, m.group(3), base_tag, italic_tag)
        pos = m.end()
    if pos < len(text):
        buffer.insert_with_tags(it, text[pos:], base_tag)


def _insert_bullet_text(
    buffer: "GtkTextBuffer",
    it: "GtkTextIter",
    text: str,
    base_tag: "GtkTextTag",
    bold_tag: "GtkTextTag",
    code_tag: "GtkTextTag",
    italic_tag: "GtkTextTag",
) -> None:
    # If the model returns "Topic: detail" instead of Markdown, still make the
    # topic scannable. Avoid over-boldening sentences with late punctuation.
    m = re.match(r"^([^:]{2,48}:)\s+(.+)$", text)
    if m and not any(ch in m.group(1) for ch in ".;?!"):
        buffer.insert_with_tags(it, m.group(1), base_tag, bold_tag)
        buffer.insert(it, " ")
        _insert_md(buffer, it, m.group(2), base_tag, bold_tag, code_tag, italic_tag)
        return
    _insert_md(buffer, it, text, base_tag, bold_tag, code_tag, italic_tag)


def _build_buffer(buffer: "GtkTextBuffer", text: str) -> None:
    from gi.repository import Pango

    font_desc = Pango.FontDescription()
    font_desc.set_family("Cantarell, Segoe UI, sans-serif")
    font_desc.set_size(9 * Pango.SCALE)

    heading_tag = buffer.create_tag(
        "heading",
        font_desc=font_desc,
        weight=Pango.Weight.BOLD,
        scale=1.18,
        foreground="#0f172a",
        pixels_above_lines=2,
        pixels_below_lines=10,
    )
    body_tag = buffer.create_tag(
        "body",
        font_desc=font_desc,
        foreground="#334155",
        pixels_above_lines=1,
        pixels_below_lines=5,
    )
    bullet_tag = buffer.create_tag(
        "bullet",
        font_desc=font_desc,
        foreground="#334155",
        pixels_above_lines=3,
        pixels_below_lines=8,
        left_margin=18,
        indent=-10,
    )
    bullet_marker_tag = buffer.create_tag(
        "bullet_marker",
        font_desc=font_desc,
        weight=Pango.Weight.BOLD,
        foreground="#2563eb",
        pixels_above_lines=3,
        pixels_below_lines=8,
    )
    bold_tag = buffer.create_tag(
        "bold",
        weight=Pango.Weight.BOLD,
        foreground="#111827",
    )
    code_tag = buffer.create_tag(
        "code",
        family="monospace",
        size=9 * Pango.SCALE,
        foreground="#9f1239",
        background="#f1f5f9",
    )
    italic_tag = buffer.create_tag(
        "italic",
        style=Pango.Style.ITALIC,
    )

    it = buffer.get_end_iter()
    for raw in text.split("\n"):
        s = raw.strip()
        if not s:
            continue
        if len(s) >= 2 and s.startswith("## "):
            # Heading — no inline markdown (already bold)
            buffer.insert_with_tags(it, s[3:], heading_tag)
        elif (
            len(s) >= 2
            and s[0] in ("*", "-", "\u2022", "\u25b8")
            and s[1] == " "
        ):
            content = s[2:].lstrip()
            buffer.insert_with_tags(it, "\u2022 ", bullet_marker_tag)
            _insert_bullet_text(
                buffer, it, content, bullet_tag, bold_tag, code_tag, italic_tag,
            )
        else:
            _insert_md(buffer, it, s, body_tag, bold_tag, code_tag, italic_tag)
        buffer.insert(it, "\n")


def show_popup(text: str) -> None:
    mouse_x, mouse_y = get_mouse_position()
    log(f"mouse at {mouse_x},{mouse_y}")

    import gi
    gi.require_version("Gtk", "3.0")
    from gi.repository import Gtk, GLib, Gdk, Pango

    PAD = 10
    ACCENT_W = 3
    CHARS_PER_LINE = 82

    css = b"""
    .card {
        background-color: #fbfdff;
        border: 1px solid #dbe4f0;
        border-radius: 14px;
    }
    .accent {
        background-color: #2563eb;
    }
    textview, textview text,
    scrolledwindow, scrolledwindow viewport,
    viewport {
        background-color: transparent;
    }
    """

    provider = Gtk.CssProvider()
    provider.load_from_data(css)
    Gtk.StyleContext.add_provider_for_screen(
        Gdk.Screen.get_default(), provider, Gtk.STYLE_PROVIDER_PRIORITY_APPLICATION
    )

    # Estimate dynamic size (account for existing newlines + line wrapping).
    # Keep the card compact; the text view scrolls for unusually long summaries.
    line_height = 17
    raw_lines = text.split("\n")
    num_lines = max(text.count("\n") + 1,
                    sum(max(1, len(l) // CHARS_PER_LINE) for l in raw_lines))
    content_w = min(max(400, CHARS_PER_LINE * 6 + 30), 600)
    content_h = min(num_lines * line_height + 34, 320)
    total_w = content_w + ACCENT_W + 2
    total_h = content_h + 20

    win = Gtk.Window(type=Gtk.WindowType.TOPLEVEL)
    win.set_default_size(total_w, total_h)
    win.set_decorated(False)
    win.set_keep_above(True)
    win.set_resizable(False)
    win.set_app_paintable(True)

    visual = win.get_screen().get_rgba_visual()
    if visual is not None:
        win.set_visual(visual)

    display = Gdk.Display.get_default()
    monitor = display.get_monitor_at_point(mouse_x, mouse_y)
    geo = monitor.get_geometry()
    sw, sh = geo.width, geo.height

    x = mouse_x + 4
    y = mouse_y + 18
    if x + total_w > sw:
        x = mouse_x - total_w - 4
    if y + total_h > sh:
        y = mouse_y - total_h - 4
    x = max(PAD, min(x, sw - total_w - PAD))
    y = max(PAD, min(y, sh - total_h - PAD))
    win.move(int(x), int(y))

    # --- Widget hierarchy ---
    # Gtk.Window (transparent)
    #   └── Gtk.EventBox (.card)       ← soft card background + rounded border
    #       └── Gtk.Box (horizontal)
    #           ├── Gtk.EventBox (.accent)  ← narrow blue accent strip
    #           └── Gtk.ScrolledWindow
    #               └── Gtk.TextView     ← transparent, margins, tags

    card = Gtk.EventBox()
    card.get_style_context().add_class("card")

    hbox = Gtk.Box(orientation=Gtk.Orientation.HORIZONTAL, spacing=0)

    accent = Gtk.EventBox()
    accent.set_size_request(ACCENT_W, -1)
    accent.get_style_context().add_class("accent")

    scrolled = Gtk.ScrolledWindow()
    scrolled.set_policy(Gtk.PolicyType.NEVER, Gtk.PolicyType.AUTOMATIC)
    scrolled.set_propagate_natural_height(True)

    text_view = Gtk.TextView()
    text_view.set_wrap_mode(Gtk.WrapMode.WORD_CHAR)
    text_view.set_editable(False)
    text_view.set_cursor_visible(False)
    text_view.set_margin_start(16)
    text_view.set_margin_end(18)
    text_view.set_margin_top(14)
    text_view.set_margin_bottom(16)

    _build_buffer(text_view.get_buffer(), text)

    scrolled.add(text_view)
    hbox.pack_start(accent, False, False, 0)
    hbox.pack_start(scrolled, True, True, 0)
    card.add(hbox)
    win.add(card)

    def _on_key(w, ev):
        if ev.keyval in (Gdk.KEY_Escape, Gdk.KEY_q, Gdk.KEY_Q):
            Gtk.main_quit()
            return True
        return False

    win.connect("key-press-event", _on_key)
    win.connect("destroy", lambda _: Gtk.main_quit())

    win.show_all()
    win.present()

    GLib.timeout_add_seconds(30, Gtk.main_quit)
    Gtk.main()


def _notify_err(msg: str) -> None:
    try:
        subprocess.run(
            ["notify-send", "--icon=dialog-error",
             "--app-name=relay", "\xd6zet Hatas\u0131", msg],
            timeout=3.0, stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL,
        )
    except Exception:
        pass


def _notify_start() -> None:
    try:
        subprocess.run(
            ["notify-send", "--icon=accessories-text-editor",
             "--app-name=relay", "\xd6zetleniyor...",
             "Se\xe7ili metin \xf6zetleniyor"],
            timeout=3.0, stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL,
        )
    except Exception:
        pass


def _notify_done(text: str) -> None:
    MAX_LEN = 100
    body = text[:MAX_LEN].replace("\n", " ").strip()
    if len(text) > MAX_LEN:
        body += "..."
    try:
        subprocess.run(
            ["notify-send", "--icon=accessories-text-editor",
             "--app-name=relay", "\xd6zet", body],
            timeout=3.0, stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL,
        )
    except Exception:
        pass


def main() -> int:
    log("=== start ===")
    _notify_start()

    text = get_selected_text()
    if not text:
        _notify_err("Metin bulunamad\u0131")
        return 1

    if len(text) > MAX_INPUT:
        _notify_err(f"Metin \xe7ok uzun ({len(text)} karakter, max {MAX_INPUT})")
        return 1

    try:
        output = summarize(text)
    except urllib.error.HTTPError as e:
        log(f"HTTP error: {e.code} {e.reason}")
        _notify_err(f"HTTP {e.code}")
        return 1
    except Exception as e:
        log(f"API error: {e}")
        _notify_err(f"API: {type(e).__name__}")
        return 1

    if not output:
        log("empty response")
        _notify_err("Groq yan\u0131t vermedi")
        return 1

    _notify_done(output)
    show_popup(output)
    log("=== done ===")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
